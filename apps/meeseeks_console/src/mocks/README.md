Mock data lives here to keep design-time fixtures isolated from real API code.

Usage:
- Set `VITE_API_MODE=mock` to force mocks.
- Default `VITE_API_MODE=auto` uses real API and falls back to mocks on first failure.
